# Email Automation Configuration
# Copy this file to .env and fill in your actual credentials
# NEVER commit the .env file to version control

# Gmail Account Settings
# Note: For Gmail, you MUST use an App Password, not your regular password
# To generate an App Password:
# 1. Enable 2-Step Verification on your Google Account
# 2. Visit: https://myaccount.google.com/apppasswords
# 3. Select 'Mail' and generate a password
# 4. Use the 16-character password below

SENDER_EMAIL=your_email@gmail.com
SENDER_PASSWORD=your_app_password_here

# Default recipient (can be overridden in code)
RECIPIENT_EMAIL=recipient_email@gmail.com

# Optional: SMTP Configuration (defaults are set for Gmail)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
