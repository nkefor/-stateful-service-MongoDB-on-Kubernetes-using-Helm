# Docs

Place screenshots and the generated demo GIF here (e.g., `docs/demo.gif`).

Suggested shots:
- Mongo Express UI home page secured by HTTPS
- GitHub Actions deploy run (successful) summary
- `kubectl get pods -n data` showing MongoDB replica set ready

